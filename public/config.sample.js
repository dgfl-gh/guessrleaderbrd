// Copy this file to public/config.js and set your real Mapbox token there.
// The production build should ensure public/config.js is never committed and is served with appropriate caching disabled.
window.GUESSR_CONFIG = {
  mapboxToken: "YOUR_MAPBOX_TOKEN_HERE"
};
